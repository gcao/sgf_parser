#!/usr/bin/env ruby

if ARGV.size == 0
  puts <<-USAGE
Usage: 
  sgf FILE_TO_PARSE
  USAGE
  exit 1
end

sgf_rb = File.expand_path(File.dirname(__FILE__) + '/../lib/sgf.rb')
if File.exists?(sgf_rb)
  require sgf_rb
else
  require 'rubygems'
  require 'sgf'
end

SGF::Parser.parse_file(ARGV[0], true)